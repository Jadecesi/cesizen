{% extends 'modalBase.html.twig' %}

{% block modal_title %}Confirmation rénisialisation mot de passe : {{ utilisateur.nom }} {{ utilisateur.prenom }}{% endblock %}

{% block modal_content %}
    <h3>Êtes-vous sûr de rénisaliser le mot de passe de cette utilisateur ?</h3>
    <br>
    <div class="modal--actions">
        <button type="button"
                data-redirect="{{ path('app_mail_renisialisation_password_admin', {'id': utilisateur.id}) }}"
                class="btn btn-danger btn-lg modalImbriquer"
                data-target="modal">
            Réinisialiser
        </button>
    </div>
{% endblock %}